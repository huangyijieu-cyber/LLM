# Tokenizer
## 1. 分词级别

分词的核心目标是：将原始文本转换为模型可以处理的离散单元（token）。根据粒度不同，主要分为以下三类

---

### 1.1 词级别（Word level）

早期的NLP系统，如基于统计机器翻译（SMT）的模型，往往采用词级别分词。这种方法最符合人类的直觉：以空格或标点符号为界，将句子切分为单词。

**机制：** 按照单词切分

**优点：** 保留了词的语义完整性，每个Token都对应一个具有明确定义的人类语言概念。

**缺陷：**
1. **导致词表爆炸：** 每个单词存在不同时态特征,这些在word level切分下都被视为独立个体,加剧了词表大小的增长.
2. **未登录词（OOV）问题：** 对未见过的词只能被映射为通用的 `<UNK>`（Unknown）标记，导致该位置的信息完全丢失。

---

### 1.2 字符级别（Character-level）

为了彻底解决OOV问题，研究者一度转向字符级别分词，将文本拆解为单个字符。

**机制：** 按照字符切分。

**优点：** 词表极小（仅需包含字母表、数字和符号，通常在100-1000量级），理论上消灭了OOV问题。

**缺陷：序列过长与语义稀疏：**
1. **计算成本：** Transformer模型的注意力机制（Self-Attention）的时间复杂度与序列长度的平方成正比（ $O(N^2)$ ）。字符级分词会导致序列长度增加5-10倍，使得训练和推理成本指数级上升。
2. **语义缺失：** 单个字符（如 "t"）通常不承载独立的语义信息。模型需要花费大量的层数和参数去组合字符以识别出“词”的概念，这浪费了模型的表达能力。

---

### 1.3 子词级别（Subword）

子词分词（Subword Tokenization）是上述两种方法的辩证统一，也是目前大语言模型的标准配置。其核心思想是：**常用词保持完整，罕见词拆分为有意义的子部件（Subword units）**。

目前主流的子词算法三巨头包括：

1. **BPE (Byte-Pair Encoding)：** 基于频率的合并策略，广泛用于GPT系列、Llama、RoBERTa。
2. **WordPiece：** 基于概率（似然度）的合并策略，起源于BERT。
3. **Unigram：** 基于概率的剪枝策略（从大词表删减），用于SentencePiece (ALBert, T5)。

---

## 2. 子词分词

### 2.1 BPE

Byte-Pair Encoding（字节对编码）最初是作为一种数据压缩算法由Philip Gage在1994年提出的。Sennrich等人于2015年将其引入NLP领域，用于解决神经机器翻译中的稀有词问题。如今，它已成为GPT系列模型的基石。

**算法逻辑：**
1. **初始化：** 将所有文本拆分为基础单元(通常为字节), 赋予每个基础单元一个单独的ID.
2. **统计：** 对要训练的数据集, 统计每对相邻的基础单元对的出现概率.
3. **合并：** 找出出现频率最高的单元对, 进行合并, 并为其分配一个新的ID, 同时对数据集中所有出现的该单元对进行替换. （此时记录下这条合并规则）
4. **迭代：** 对步骤2, 3如此进行循环.

BPE算法中最重要的是步骤3所记录的**合并规则**, 它将指导模型在分词时按照合并规则的优先级对相应单元对进行合并, 从而分词。

---

### 2.2 WordPiece

WordPiece算法由Google开发，是BERT模型的核心。它的整体流程与BPE非常相似（自底向上的合并），仅在合并标准上不同。

**BPE标准：** 选择频率最高的单元对进行合并。
**WordPiece标准：** 选择合并后使训练数据**似然度增加最多**的单元对合并。

数据似然的增加量近似为：

$$
\Delta L \approx \log \frac{P(xy)}{P(x)P(y)}
$$

即：

$$
\text{PMI}(x, y) = \frac{P(xy)}{P(x)P(y)}
$$

的log形式. 由于 $\log$ 的单调性，这等价于选择 **点互信息（PMI）最高** 的单元对。

**PMI相对于频率的优点：**
由于分母为 $P(x) \cdot P(y)$ ，故PMI相比于单纯 $P(xy)$ 更倾向于合并**内在关联性强的单元对**（即凡一出现就经常粘在一起的单元，而非本身两者均为高频词所以有很大概率相邻）。相较于BPE更加合理。

---

### 2.3 Unigram

Unigram Tokenization（主要在SentencePiece库中实现）的思路与前两者截然相反。BPE和WordPiece是**自底向上（Bottom-up）的构造，Unigram是自顶向下（Top-down）**的。

Unigram模型假设每个Subword的出现是独立的。一个句子 $X$ 被切分为序列 $x = (x_1, ..., x_m)$

的概率为：

$$
P(x) = \prod_{i=1}^{m} P(x_i)
$$

其中 $P(x_i)$ 是子词 $x_i$ 的发生概率。

**算法逻辑：**
1. **初始化：** 根据要训练的数据集, 构建一个巨大的词表(一般包含所有单字符以及数据集中常见的子串). 并对词表中单元的概率进行初始化(统计每个单元在数据集中出现频次, 进行归一化)。
2. **E-step：** 在当前词表基础上, 对每个句子都存在数个可能的切分路径, 基于Viterbi算法对每个句子进行最优切分（找到使联合概率 $P(x)$ 最大的切分路径）。
3. **M-step：** 对所有句子进行最优切分后, 重新计算每个单元的概率。
4. **计算Loss并剪枝：** 计算每个单元被移除后总似然度会下降多少（Loss）, 剪枝Loss最小的单元（通常每轮移除20%）。
5. **迭代：** 对步骤2, 3, 4进行循环, 直到词汇表大小缩小到预定大小。

### 2.4 算法比较
| 特性 | BPE (GPT-2/3/4, Llama) | WordPiece (BERT) | Unigram (T5, ALBERT) |
|------|------------------------|------------------|----------------------|
| 构建方向 | 自底向上 (Bottom-up) | 自底向上 (Bottom-up) | 自顶向下 (Top-down) |
| 核心指标 | 频率 (Frequency) | 似然度增益 / PMI | 似然度损失 (Loss) |
| 训练复杂度 | 较低 | 高 (每步需重算所有Pair得分) | 较高 (需EM迭代) |
| OOV处理 | 字节回退 (Byte fallback) | 字符回退 (需UNK Token) | 字符回退 |
| 分词确定性 | 确定性 (Deterministic) | 确定性 | 概率性 (可采样多种结果) |
| 适用场景 | 生成式模型 (Generative) | 理解式模型 (NLU) | 需正则化的任务 |

处理特殊token
tiktoken, huggingface

## 3. 子词切分的额外操作

### 3.1 预分词

BPE算法本身是“盲目”的。如果不加干预，它可能会跨越标点和单词的边界进行合并。例如，它可能会把句子结尾的 "dog." 中的 "g." 合并为一个Token。这通常是不希望看到的，因为标点符号通常有独立的语法意义。

因此，在运行BPE合并之前，需要进行一项**预分词**操作, 即先用**正则表达式(Regex)** 将文本切分为一个个基础的“单词块”。BPE只能在这些块内部进行合并，不能跨块。

### 3.2 特殊Token的处理

在代码实现中，特殊Token（如 <|endoftext|>, <PAD>, <MASK>, <|im_start|>) 的处理往往是初学者最容易混淆的地方，也是导致模型产生幻觉或安全漏洞的常见原因。
假设我们的特殊Token是 <|endoftext|>，用于标记文本结束。如果我们把它当作普通文本传给BPE算法, 算法没法识别它, 只能对其进行拆分.

解决方案:
1. Tiktoken(GPT-4 使用的): Tiktoken 要求用户显式传递 allowed_special 参数。这个参数用于控制是否运行用户输入 prompt 中存在特殊 Token. 当用户输入 Prompt 时, 会先与系统指令分离, 然后对其进行预分词时, 抽离出所有的特殊 Token. 若 allowed_special 为True, 则为每个特殊 Toekn 赋予 ID, 并放回 Prompt 进行后续 BPE 合并; 若为 False, 则将其视为普通 Token 供 BPE 切分. 随后进行 BPE 合并.
2. Huggingface Tokenizers: Huggingface 允许通过 AddedToken 对象手动添加特殊 Token. 故 Tokenizer 对象具有两个属性: special_tokens(模型内部定义的特殊 Token)以及 additional_special_tokens(用户自定义的特殊 Token). 其余流程与 Tiktoken 类似.

## 4. 总结
目前大模型的分词流程已形成一套稳定范式：

归一化 → 预分词（含特殊 Token 保护） → 子词分词 → 后处理

1. 归一化: 将原始文本转换为标准形式, 通常包含 **Unicode 规范化**, **大小写转换**, **去除多余空格**, **特殊字符处理**等.
2. 预分词: 对归一化后的文本, 先对特殊Token进行提取, 然后按空格、标点符号、数字等边界切分, 生成词单元列表.
3. 子词分词: 根据 BPE, WordPiece, Unigram 等算法切分.
4. 后处理: 将子词 Token 序列整理为模型输入所需的最终格式, 通常包括**添加特殊 Token**, **拼接多个序列**, **生成 attention mask**, **截断与填充**等.